# Tarun Siddhartha M — Professional Digital CV v2

Replace `index.html`, `style.css`, and `script.js` in `sid-1608/sid.github.io`.
Optional: add your PDF as `resume.pdf` in the repository root.

No build step is required. GitHub Pages can serve these files directly.
